# Kling数据处理 - 修改总结与后续思路

## ✅ 已完成的修改

### 1. load_kling_data.py 的主要改进

**改进内容**:

1. **SQL优化**:
   - 添加了 `ORDER BY user_id, time_stamp` 确保数据按时间顺序排序
   - 将硬编码的日期改为参数化的 `{p_date}`，提高代码可维护性

2. **输出格式改进**:
   - 从CSV改为TSV格式（制表符分隔）
   - 避免了CSV中逗号和引号导致的解析问题
   - 添加了UTF-8-sig编码，防止BOM问题

3. **文档和提示**:
   - 添加了详细的函数文档注释
   - 增强了控制台输出信息的可读性
   - 添加了下一步操作提示

**使用方法**:
```bash
python load_kling_data.py
```

输出文件：`{日期}.tsv`（例如：20251105.tsv）

---

### 2. process_kling_data.py 的主要改进

**改进内容**:

1. **统计信息增强**:
   - 添加了行为类型分布统计（RECOMMEND/SEARCH/PRODUCE）
   - 添加了搜索场景统计（element_query_content使用情况）
   - 添加了用户序列长度分布统计
   - 所有数字都使用千位分隔符，更易读

2. **输出格式美化**:
   - 使用分隔线和表情符号美化输出
   - 清晰的阶段划分和进度提示
   - 最后提供下一步操作建议

3. **数据质量**:
   - 保持原有的数据处理逻辑
   - 确保生成的三个文件格式正确

**生成的三个核心文件**:

1. **kling_items.json** - 物品元数据
   ```json
   {
     "item_id": {
       "title": "标题",
       "description": "描述（来自prompt）",
       "categories": "Image Creation > Material",
       "sid": "<|sid_begin|><s_a_69><s_b_142><s_c_246><|sid_end|>"
     }
   }
   ```

2. **kling_sequential.txt** - 用户行为序列
   ```
   user_id item_id1 item_id2 item_id3 ...
   ```

3. **kling_user_behaviors.json** - 用户行为详情（富文本信息）
   ```json
   {
     "user_id": [
       {
         "item_id": "xxx",
         "event_type": "SEARCH",
         "behavior_type": "...",
         "behavior_subtype": "...",
         "element_query_content": "搜索词",
         "query_cnt": 5
       }
     ]
   }
   ```

**使用方法**:
```bash
python process_kling_data.py
```

---

## 📋 后续需要创建的三个脚本

根据论文附录A.3.1的三个任务，你需要创建以下三个脚本：

### 脚本1: generate_kling_RA_data.py

**对应任务**: Interleaved User Persona Grounding（用户画像与行为交织）

**核心目标**: 生成富文本的用户画像描述

**输入文件**:
- `kling_items.json` → 获取物品的sid、title、categories
- `kling_sequential.txt` → 获取用户行为序列
- `kling_user_behaviors.json` → 获取行为类型、搜索词等富文本信息

**输出文件**:
- `training_RA_train.parquet`
- `training_RA_val.parquet`
- `training_RA_test.parquet`

**关键思路**:

Kling数据相比Beauty数据最大的优势是有丰富的行为类型，要充分利用这一点。

参考论文中的示例格式，生成类似这样的用户画像：

```
# User Profile Narrative
The user recently searched for "AI video generation" 3 times;

# Like Behavior
He recently liked video <|sid_begin|><s_a_1><s_b_2><s_c_3><|sid_end|>, 
titled "Amazing AI Art", its categories are "Image Creation > Material";

# Comment Behavior
He recently commented on video <|sid_begin|><s_a_4><s_b_5><s_c_6><|sid_end|>, 
a content about "Video Creation > Short Film";

# Produce Behavior
He recently created content with prompt "A beautiful landscape scene", 
video <|sid_begin|><s_a_7><s_b_8><s_c_9><|sid_end|>;
```

**实现要点**:

1. **行为类型映射**:
   ```python
   behavior_map = {
       'LIKE': 'liked',
       'UNLIKE': 'unliked', 
       'COMMENT': 'commented on',
       'SHARE': 'shared',
       'VIDEO_PLAY_FINISH': 'finished watching',
       'LONG_PLAY': 'watched',
       'PRODUCE': 'created'
   }
   ```

2. **搜索行为特殊处理**:
   - 使用 `element_query_content` 字段
   - 使用 `query_cnt` 字段表示搜索次数
   - 例如："The user searched for 'AI video' 3 times"

3. **生产行为特殊处理**:
   - PRODUCE类型的行为要包含prompt信息
   - 例如："created content with prompt 'xxx'"

4. **数据分割策略**:
   - train: 使用前N-2个物品生成描述，预测第N-1个
   - val: 使用前N-1个物品生成描述，预测第N个
   - test: 使用全部N个物品生成描述

**参考脚本**: `/workspace/data/generate_RA_data.py`

---

### 脚本2: generate_kling_sid_prediction_data.py

**对应任务**: Sequential Preference Modeling（序列推荐）

**核心目标**: 预测用户下一个交互的物品SID

**输入文件**:
- `kling_items.json` → 获取物品的sid
- `kling_sequential.txt` → 获取用户行为序列

**输出文件**:
- `training_prediction_sid_data_train.parquet`
- `training_prediction_sid_data_val.parquet`
- `training_prediction_sid_data_test.parquet`

**关键思路**:

这个任务相对简单，主要是将物品ID序列转换为SID序列。

生成格式：
```python
{
    "user_id": "xxx",
    "description": "The user has interacted with: <|sid_begin|>...<|sid_end|>; <|sid_begin|>...<|sid_end|>;",
    "groundtruth": "<|sid_begin|><s_a_X><s_b_Y><s_c_Z><|sid_end|>"
}
```

**实现要点**:

1. 只需要SID序列，不需要行为类型
2. 使用简单的模板："The user has interacted with the following items: ..."
3. 数据分割与脚本1相同

**参考脚本**: `/workspace/data/generate_sid_prediction_data.py`

**提示**: 这个脚本几乎可以直接复用Beauty数据的脚本，只需修改文件路径

---

### 脚本3: generate_kling_training_data.py

**对应任务**: Itemic Dense Captioning（物品描述生成）

**核心目标**: 生成包含物品详细信息的描述

**输入文件**:
- `kling_items.json` → 获取物品的sid、title、categories
- `kling_sequential.txt` → 获取用户行为序列

**输出文件**:
- `training_align_data_train.parquet`
- `training_align_data_val.parquet`
- `training_align_data_test.parquet`

**关键思路**:

组合物品的sid、title、categories信息，生成完整的描述。

生成格式：
```python
{
    "user_id": "xxx",
    "description": "The user has interacted with: <|sid_begin|>...<|sid_end|>, its title is 'xxx', its categories are 'xxx'; ..."
}
```

**实现要点**:

1. 包含sid、title、categories三个信息
2. 格式：`{sid}, its title is "{title}", its categories are "{categories}"`
3. 数据分割与脚本1相同

**参考脚本**: `/workspace/data/generate_training_data.py`

**提示**: 这个脚本也相对简单，主要是信息组合

---

## 🎯 实现优先级建议

### 第一优先级：脚本2和3
- 相对简单，可以快速实现
- 可以直接参考现有Beauty数据的脚本
- 验证数据流程是否正确

### 第二优先级：脚本1
- 最复杂，需要仔细设计
- 是Kling数据的核心优势所在
- 需要充分利用富文本行为信息

---

## 🔑 关键差异点

### Kling vs Beauty 数据的主要区别

| 维度 | Beauty数据 | Kling数据 |
|------|-----------|-----------|
| **行为类型** | 仅"购买"一种 | 搜索、点赞、评论、生产等多种 |
| **SID维度** | 4维 (s_a, s_b, s_c, s_d) | 3维 (s_a, s_b, s_c) |
| **富文本信息** | 简单 | 丰富（搜索词、查询次数、创作prompt） |
| **类别层级** | Beauty > Hair Care | Video Creation > Short Film |

### 需要注意的地方

1. **SID格式**:
   - Kling: `<|sid_begin|><s_a_X><s_b_Y><s_c_Z><|sid_end|>`
   - Beauty: `<|sid_begin|><s_a_X><s_b_Y><s_c_Z><s_d_W><|sid_end|>`

2. **行为描述**:
   - Beauty只能写："The user has purchased..."
   - Kling可以写："The user searched..., liked..., commented..., created..."

3. **搜索场景**:
   - Beauty没有搜索场景
   - Kling有搜索词和搜索次数，要充分利用

---

## 📂 文件组织

当前kling_data目录结构：

```
kling_data/
├── load_kling_data.py              # ✅ 已完成
├── process_kling_data.py           # ✅ 已完成
├── README.md                        # ✅ 技术文档（英文）
├── NEXT_STEPS.md                    # ✅ 实现指南（英文）
├── 总结.md                          # ✅ 本文档（中文）
│
├── generate_kling_RA_data.py       # ❌ 待创建
├── generate_kling_sid_prediction_data.py  # ❌ 待创建
└── generate_kling_training_data.py # ❌ 待创建
```

---

## 🚀 完整工作流程

```
第一步：获取原始数据
python load_kling_data.py
   ↓
输出：{日期}.tsv

第二步：生成元数据
python process_kling_data.py
   ↓
输出：kling_items.json
      kling_sequential.txt
      kling_user_behaviors.json

第三步：生成训练数据（待实现）
python generate_kling_RA_data.py
python generate_kling_sid_prediction_data.py
python generate_kling_training_data.py
   ↓
输出：9个.parquet文件（3个脚本 × 3个数据集）

第四步：训练模型（使用现有训练脚本）
bash run_training_stage1.sh
bash run_training_stage2.sh
```

---

## 💡 实现建议

### 1. 增量开发策略

- **阶段1**: 先实现基础版本，生成简单的描述文本
- **阶段2**: 验证数据格式正确性，确保能被训练脚本读取
- **阶段3**: 逐步丰富文本质量，加入更多行为类型
- **阶段4**: 优化文本模板，提高可读性

### 2. 数据验证检查清单

生成数据后，务必检查：
- [ ] SID格式是否正确（3维，不是4维）
- [ ] 文本长度是否合理（不超过token限制）
- [ ] train/val/test数量是否正确
- [ ] 是否有缺失值（None/NaN）
- [ ] parquet文件是否能正常读取
- [ ] 与Beauty数据格式是否一致

### 3. 复用现有代码

三个待实现的脚本可以大量复用`/workspace/data/`目录下的现有代码：

- 读取文件的代码 → 完全复用
- 数据分割的逻辑 → 完全复用
- DataFrame生成和保存 → 完全复用
- 主要修改点 → 文本生成逻辑

### 4. 调试技巧

- 先用小样本（10-100个用户）测试
- 打印生成的前几条样本检查格式
- 对比Beauty数据和Kling数据的输出格式
- 使用pandas读取parquet文件验证

---

## 📊 预期数据量

假设处理1天的Kling数据：

- 原始记录：~10万条
- 唯一物品：~3万个
- 唯一用户：~1万个
- 有效用户（序列≥3）：~5千个

生成的训练数据：
- train: ~5千条
- val: ~5千条
- test: ~5千条

每个任务生成一套，共3套 × 3个文件 = 9个parquet文件

---

## 🔗 相关文档链接

- **README.md**: 详细的技术文档和数据格式说明
- **NEXT_STEPS.md**: 具体的实现指南和代码示例
- **论文附录A.3.1**: 三个任务的详细说明
- **/workspace/data/**: Beauty数据的参考脚本

---

## ❓ 常见问题

### Q1: 为什么用TSV而不是CSV？
A: prompt字段可能包含逗号和引号，TSV（制表符分隔）更不容易出错。

### Q2: SID维度不同会影响训练吗？
A: 会的，需要在词表扩展时只添加s_a、s_b、s_c三个维度，不需要s_d。

### Q3: 三个任务的数据可以混合训练吗？
A: 通常是分阶段训练：先Alignment，再SID预测，最后Dense Captioning。

### Q4: 如何验证生成的数据质量？
A: 随机抽取样本人工检查，确保文本通顺、SID格式正确、信息完整。

### Q5: 搜索行为一定要包含吗？
A: 不是必须的，但这是Kling数据的优势，建议充分利用。

---

## 📞 下一步

你现在可以：

1. ✅ 运行 `load_kling_data.py` 获取数据
2. ✅ 运行 `process_kling_data.py` 生成元数据
3. ❌ 根据 `NEXT_STEPS.md` 的指导创建三个数据生成脚本

如果有任何问题，参考：
- 技术细节 → README.md
- 实现指南 → NEXT_STEPS.md
- 参考代码 → /workspace/data/

祝实现顺利！ 🎉
